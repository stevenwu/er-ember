<div class='container-fluid'>

<nav class='navbar navbar-default navbar-fixed-top' role='navigation'>
  <div class='container'>
    {{link-to 'Stamford' 'application' class='navbar-brand'}}
    <ul class='nav navbar-nav'>
    {{#nav-item}}
      {{#link-to 'about' tagName='li'}}
        <a href='#'>About</a>
      {{/link-to}}
    {{/nav-item}}
    {{#nav-item}}
      {{#link-to 'users' tagName='li'}}
        <a href='#'>Users</a>
      {{/link-to}}
    {{/nav-item}}
    </ul>

    {{#unless currentUser}}
      {{link-to 'Log In' 'login' class='btn btn-default navbar-btn navbar-right'}}
    {{/unless}}

    {{#if currentUser}}


    <ul class='nav navbar-nav navbar-right'>

    {{#nav-item}}
      <a class='dropdown-toggle' data-toggle='dropdown' href='#'>
      {{currentUser.email}}<span class='caret'></span>
      </a>

      <ul class='dropdown-menu' id='dropdown'>
        {{#nav-item}}
          <a {{action 'signOut'}}>Sign out</a>
        {{/nav-item}}
      </ul>
    {{/nav-item}}
    </ul>

    {{/if}}
  </div>
</nav>
<h2 id='title'>Welcome to Oxford Circus</h2>
<div class='container'>
{{outlet}}
</div>
</div>
