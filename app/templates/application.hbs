<div class='container-fluid'>

<nav class='navbar navbar-default navbar-fixed-top' role='navigation'>
  <div class='container'>
    {{link-to 'Stamford' 'application' class='navbar-brand'}}
    <ul class='nav navbar-nav'>
    {{#if currentUser}}
      {{#nav-item}}
        {{#link-to 'dashboard' tagName='li'}}
          <a href='#'>Dashboard</a>
        {{/link-to}}
      {{/nav-item}}
    {{/if}}
    {{#nav-item}}
      {{#link-to 'about' tagName='li'}}
        <a href='#'>About</a>
      {{/link-to}}
    {{/nav-item}}
    {{#nav-item}}
      {{#link-to 'users' tagName='li'}}
        <a href='#'>Users</a>
      {{/link-to}}
    {{/nav-item}}
    </ul>

    {{#unless currentUser}}
      {{link-to 'Log In' 'login' class='btn btn-default navbar-btn navbar-right' active=false}}
    {{/unless}}

    {{#if currentUser}}


    <ul class='nav navbar-nav navbar-right'>

    {{#nav-item}}
      <a class='dropdown-toggle' data-toggle='dropdown' href='#'>
      {{currentUser.email}}<span class='caret'></span>
      </a>

      <ul class='dropdown-menu' id='dropdown'>
        {{#nav-item}}
          <a class='sign-out-link' {{action 'signOut'}}>Sign out</a>
        {{/nav-item}}
      </ul>
    {{/nav-item}}
    </ul>

    {{/if}}
  </div>
</nav>

  <div class='container'>
    {{outlet}}
  </div>
</div>
